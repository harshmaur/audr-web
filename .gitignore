node_modules/
dist/
.astro/
.cache/
.env
.env.local
.env.*.local
*.log
.DS_Store

# WASM build output is not committed; CI rebuilds it from the audr submodule.
public/wasm/*.wasm
public/wasm/wasm_exec.js

# Polished sample report is generated; CI rebuilds it on every deploy.
public/sample-report.html

# Playwright + Lighthouse artifacts
playwright-report/
test-results/
.lighthouseci/

# Coverage
coverage/

# Build-time generated metadata (e.g. WASM blob SHA-256). Regenerated by
# scripts/build-wasm.sh on every build, so committing would just churn.
src/generated/

# Vercel link state — committed to the repo would leak the orgId scoping.
# Add to git only if you want every contributor to deploy to the same project.
.vercel
